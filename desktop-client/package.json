{
  "name": "@pomoremote/desktop-client",
  "version": "0.1.0",
  "private": true,
  "description": "Thin desktop CLI and service client for the PomoRemote Android app.",
  "type": "module",
  "bin": {
    "pomo": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "engines": {
    "node": ">=20"
  }
}
